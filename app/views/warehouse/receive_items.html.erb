<% content_for(:custom_js, javascript_include_tag('receive_items')) %>
<h1>Receiveing</h1>

<%= form_tag("/receivings/new") %>
<div class="box">
	<table>
	    <tr>
		    <td valign="top">
    			<label for="vendors">From:</label>
    		</td>
    		<td>
    			<select id="vendors" name="vendors" size="10">
    				<% @vendor_names.each do |vn| %>
    				<option value="<%= vn %>"><%= vn %></p>
    				<% end #each %>
    			</select>
    		</td>
    		<td valign="top">
    			<label for="product_variants">Variants:</label>
    		</td>
    		<td>
    			<div id="productVariants"></div>
    	    </td>
	    </tr>
	</table>
</div>
<p><input type="submit" value="Continue &rarr;"></p>
</form>
